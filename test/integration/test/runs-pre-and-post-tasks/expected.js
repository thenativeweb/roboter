'use strict';

const exitCode = 0;

const stdout = [
  `Hello from pre task!`,
  `Hello from post task!`,
  `✓ Tests of type units successful.`
];

const stderr = '';

module.exports = { exitCode, stdout, stderr };
