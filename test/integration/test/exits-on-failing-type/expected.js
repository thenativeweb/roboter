'use strict';

const exitCode = 0;

const stdout = [
  `✓ Tests of type baz successful.`,
  `✓ Tests of type foobar successful.`
];

const stderr = '';

module.exports = { exitCode, stdout, stderr };
