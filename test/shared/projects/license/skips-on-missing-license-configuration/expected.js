'use strict';

const exitCode = 0;

const stdout = ``;

const stderr = 'No list of compatible licenses found. Skipping license compatibility check...';

module.exports = { exitCode, stdout, stderr };
