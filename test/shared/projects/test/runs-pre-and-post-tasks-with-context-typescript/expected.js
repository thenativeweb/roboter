'use strict';

const exitCode = 0;

const stdout = [
  `Hello from pre task!`,
  `Hello from external!`,
  `Hello from post task!`,
  `unit tests successful.`
];

const stderr = '';

module.exports = { exitCode, stdout, stderr };
