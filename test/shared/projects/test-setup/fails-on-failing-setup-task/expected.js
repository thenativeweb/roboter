'use strict';

const subCommand = 'test setup'

const exitCode = 1;

const stdout = '';

const stderr = [
  'Error: Setup failed.',
  '✗ Failed to run test setup.'
];

module.exports = { subCommand, exitCode, stdout, stderr };
