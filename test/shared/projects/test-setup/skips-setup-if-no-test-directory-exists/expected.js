'use strict';

const subCommand = 'test setup'

const exitCode = 0;

const stdout = '';

const stderr = [
  `No test directory found, skipping test setup.`
];

module.exports = { subCommand, exitCode, stdout, stderr };
