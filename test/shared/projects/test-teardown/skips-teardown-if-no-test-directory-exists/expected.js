'use strict';

const subCommand = 'test teardown'

const exitCode = 0;

const stdout = '';

const stderr = [
  `No test directory found, skipping test teardown.`
];

module.exports = { subCommand, exitCode, stdout, stderr };
